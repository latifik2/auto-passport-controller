apiVersion: v1
kind: Service
metadata:
  name: {{ include "auto-passport.fullname" . }}
  labels:
    {{- include "auto-passport.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auto-passport.selectorLabels" . | nindent 4 }}
